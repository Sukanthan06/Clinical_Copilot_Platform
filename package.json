{
  "name": "clinical-copilot-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "install": "npm --prefix frontend install",
    "dev": "npm --prefix frontend run dev"
  },
  "dependencies": {
    "axios": "^1.18.1"
  }
}
